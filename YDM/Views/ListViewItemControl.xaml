<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:ff="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms" x:Class="YDM.Views.ListViewItemControl">
    <ContentView.Content>
        <Grid HorizontalOptions="FillAndExpand" BackgroundColor="#333" Margin="8">
            <ff:CachedImage Aspect="AspectFill" Margin="0" DownsampleToViewSize="true" Source="{Binding Image}" />
            <StackLayout VerticalOptions="CenterAndExpand" Padding="26" HorizontalOptions="FillAndExpand" BackgroundColor="#7f000000">
                <Label Text="{Binding Title}" VerticalOptions="Center" TextColor="White" FontSize="20" MaxLines="1" HorizontalOptions="Center" />
                <Label Text="{Binding Channel}" TextColor="White" FontSize="Small" VerticalOptions="Center" HorizontalOptions="Center" />
                <Label Text="{Binding Duration}" TextColor="White" FontSize="Small" VerticalOptions="Center" HorizontalOptions="Center" />
            </StackLayout>
        </Grid>
    </ContentView.Content>
</ContentView>